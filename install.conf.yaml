- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.gitconfig:

- create:
    - ~/Work

- shell:
    - 
      command: ./setup_homebrew.zsh
      stdout: true
      sterr: true